import { SortListDirective } from './sort-list.directive';

describe('SortListDirective', () => {
  it('should create an instance', () => {
    const directive = new SortListDirective();
    expect(directive).toBeTruthy();
  });
});
